import c3d
import numpy as np
import c3d2csv
import csv


#reader = c3d.Reader(open('nzgsm.c3d', 'rb'))
#c3d2csv(reader)
print(int(2-2.5+0.6))
# reader = c3d.Reader(open('nzgsm.c3d', 'rb'))
# #for i, points, analog in reader.read_frames():
# #    print('frame {}: point {}, analog {}'.format(i, points.shape, analog.shape))

# with open('test.csv','w')as f:
#     f_csv = csv.writer(f) 
#     f_csv.writerows(reader.read_frames())

